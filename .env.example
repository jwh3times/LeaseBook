# Optional pgAdmin overrides. These are safe local defaults; repository-specific values are stored
# in 1Password and injected at runtime rather than copied into a plaintext .env.
LEASEBOOK_PGADMIN_EMAIL=admin@leasebook.com
LEASEBOOK_PGADMIN_PASSWORD=admin
