# Dependabot version updates. Keeps every dependency ecosystem in the repo current.
# Security updates are enabled separately in repo Settings → Code security; the weekly
# version-update sweep below also picks up fixed advisories as they ship.
#
# Noise control: minor + patch bumps are grouped into one PR per ecosystem; major
# versions open individual PRs so breaking changes get reviewed on their own.
# Docs: https://docs.github.com/code-security/dependabot/dependabot-version-updates
version: 2
updates:
  # .NET — host + module projects + tests. NuGet discovery walks the repo from root and
  # finds every *.csproj referenced by LeaseBook.slnx (src/ and tests/).
  - package-ecosystem: "nuget"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
      - "nuget"
    commit-message:
      prefix: "build(deps)"
    groups:
      dotnet-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"

  # React + TypeScript SPA (package.json / package-lock.json live in web/).
  - package-ecosystem: "npm"
    directory: "/web"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
      - "npm"
    commit-message:
      prefix: "build(deps)"
    groups:
      npm-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"

  # Base images in the production Dockerfile (FROM lines).
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "docker"
    commit-message:
      prefix: "build(deps)"
    groups:
      docker-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"

  # Service images in docker-compose.yml (postgres, pgadmin). Separate ecosystem from
  # "docker" since GA in Feb 2025.
  - package-ecosystem: "docker-compose"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "docker-compose"
    commit-message:
      prefix: "build(deps)"
    groups:
      compose-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"

  # Action versions pinned in .github/workflows/*.yml (ci, deploy-dev, deploy-prod).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "ci(deps)"
    groups:
      actions-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
